title: WinPanel
description: >-
  Free control panel for hosting websites, Node.js apps, PHP, WordPress and game servers
  on Windows Server 2025/2022 and Windows 11 - Minecraft and Steam dedicated servers,
  HTTPS, DNS, email and customer logins, without IIS.

url: https://decerto.github.io
baseurl: /winpanel

# social card for pages that do not set their own
image: /winpanel/banner.png

theme: jekyll-theme-cayman

plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-relative-links

# README.md stays the index of the docs folder on github.com; index.md is the
# index of the published site.
exclude:
  - README.md

defaults:
  - scope:
      path: ""
    values:
      layout: default
